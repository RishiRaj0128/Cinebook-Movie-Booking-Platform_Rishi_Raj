{
  "name": "cinebook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix cinebook-frontend run dev",
    "start": "npm --prefix cinebook-frontend run dev",
    "build": "npm --prefix cinebook-frontend run build"
  }
}
